def count_numbers(which, numbers):
    if which != "even" and which != "odd":
        return -1

    count = 0
    for num in numbers:
        if which == "even" and num % 2 == 0:
            count += 1
        elif which == "odd" and num % 2 != 0:
            count += 1
    return count
